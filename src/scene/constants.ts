import type { PlaneId } from './types';

export const CUBE_SIZE = 5;
export const HALF_CUBE_SIZE = CUBE_SIZE / 2;
export const PLANE_SIZE = CUBE_SIZE * 1.1;
export const PLANE_OFFSET = 0.05;
export const DEFAULT_SELECTED_PLANE_ID: PlaneId = 'pz';
